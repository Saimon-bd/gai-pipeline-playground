name: terraform-cd

on:
  workflow_call:
    secrets:
      github-token:
        required: true
      azure-credentials-env:
        required: true
      azure-credentials-adb2c:
        required: false
      azure-shared-subscription-id:
        required: true
    inputs:
      region:
        required: true
        type: string
      team:
        required: true
        type: string
      environment:
        required: true
        type: string
      terraform-version:
        required: true
        type: string

jobs:
  terraform-plan:
    name: Terraform Plan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          persist-credentials: false

      - name: Fake Terraform Plan
        run: echo "✅ Terraform plan completed (placeholder)"

  terraform-apply:
    name: Terraform Apply
    runs-on: ubuntu-latest
    needs: [terraform-plan]
    strategy:
      matrix:
        environment: ${{ fromJson(inputs.environment) }}
    environment: ${{ matrix.environment }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          persist-credentials: false

      - name: Wait for Approval
        run: echo "⏳ Waiting for approval for env=${{ matrix.environment }}"

      - name: Fake Terraform Apply
        run: echo "🚀 Terraform apply completed for ${{ matrix.environment }} (placeholder)"
